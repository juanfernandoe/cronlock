```

curl https://raw.githubusercontent.com/juanfernandoe/cronlock/master/lock-and-repeat?v=$(date +%s) > /usr/bin/lock-and-repeat && chmod 755 /usr/local/bin/lock-and-repeat


curl https://raw.githubusercontent.com/juanfernandoe/cronlock/master/cronlock?v=$(date +%s) > /usr/bin/cronlock && chmod 755 /usr/local/bin/cronlock
```
